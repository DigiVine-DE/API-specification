# GET /terrain/{TerrainId}
operationId: getOneTerrain
parameters:
  TerrainId: "$response.body#/id"
description: >
  The `id` value returned in the response can be used as
  the `id` parameter in `GET /terrain/{TerrainId}`
  (see also `getOneTerrain`).
  Returns a specific stored Terrain.