# DELETE /harvest/complete/{HarvestTaskId}
operationId: deleteOneCompletedHarvestTask
parameters:
  HarvestTaskId: "$response.body#/id"
description: >
  The `id` value returned in the response can be used as
  the `id` parameter in `DELETE /harvest/complete/{HarvestTaskId}`
  (see also `deleteOneCompletedHarvestTask`).
  Deletes a specific stored completed Harvest Task or a specific stored completed Harvest with Harvest Measurements.