# GET /forecast/{ForecastId}
operationId: getOneForecast
parameters:
  ForecastId: "$request.path.ForecastId"
description: >
  The `id` value given in the request can be used as
  the `id` parameter in `GET /forecast/{ForecastId}`
  (see also `getOneForecast`).
  Returns a specific stored Forecast.